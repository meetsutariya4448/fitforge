# Base URL for the FastAPI backend (used in axios client)
# In development the Vite proxy handles /api/* so this isn't needed,
# but set it for production builds.
VITE_API_BASE_URL=http://localhost:8000
